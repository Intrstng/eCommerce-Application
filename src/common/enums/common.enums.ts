/**
 * Some COMMON enums
 * ToDO: add common enums (HTTP Methods, HTTP Statuses, etc.)
 */

export enum Http_Methods {
    GET = 'GET',
    POST = 'POST',
    PUT = 'PUT',
    PATCH = 'PATCH',
    DELETE = 'DELETE',
}
