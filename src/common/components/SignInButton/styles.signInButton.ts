import { genStyles } from '../../utils/generate-styles';

export const STYLES = genStyles({
    signInBtn: {
        // width: '3rem',
    },
    signInIcon: {
        // color: 'rgba(210,10,17,0.6)',
    },
    authLinkDisabled: {
        opacity: '0.3',
    },
});
