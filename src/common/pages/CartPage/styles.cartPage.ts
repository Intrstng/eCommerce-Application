import { genStyles } from '../../utils/generate-styles';

export const STYLES = genStyles({
    cartItemTitle: {
        marginBottom: '0.5rem',
        fontFamily: 'Jost, sans-serif',
        fontSize: '1rem',
        fontWeight: '400',
        lineHeight: '1.5rem',
        color: '#161412',
    },
});
